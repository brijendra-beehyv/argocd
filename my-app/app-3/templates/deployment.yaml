---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: my-helm-app
  namespace: {{ .Values.namespace }}
spec:
  selector:
    matchLabels:
      app: my-helm-app
  template:
    metadata:
      labels:
        app: my-helm-app
    spec:
      containers:
      - name: my-helm-app
        image: {{ .Values.image.repo }}:{{ .Values.image.tag }}
        ports:
        - containerPort: 80
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 250m
            memory: 256Mi
